node_modules/
dist/
.env
.env.*
!.env.example
*.log
logs/
.DS_Store
.vite/

# Per-build memory databases (multi-graph-memory)
.multi-memory/

# Design review scratch — served by vite, never shipped
public/memory-preview.html

# Typecheck/preview scratch. Inside the repo on purpose: tsc's upward node_modules
# walk finds the real @types from here, which /tmp cannot reach.
.preview-work/

# Auth integration packs. These carry a live OAuth client secret in plaintext and
# this repo is public — the values belong in .env (ignored above) and nowhere else.
# Never committed; keep it that way.
multi-app-auth-integration.md
multi-app-how-to-integrate-auth.md
.multi-memory.v0/

# Reference material pulled in for the harness-upgrade analysis (Gemini API docs) —
# research input, not this project's own documentation.
docs/
