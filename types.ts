// This file has been replaced by the new `types` directory.
// Type definitions are now split into `types/ai.ts`, `types/chat.ts`, and `types/project.ts`.
// They are all exported from `types/index.ts`.
